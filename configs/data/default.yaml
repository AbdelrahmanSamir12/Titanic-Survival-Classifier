defaults:
  - _self_
  - /model@model

data:
  raw_path: "data/raw/train.csv"
  processed_path: "data/processed"
  test_size: 0.2
  random_state: 42
  features:
    - Pclass
    - Sex
    - Age
    - SibSp
    - Parch
    - Fare
    - Embarked
  target: Survived